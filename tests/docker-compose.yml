services:
  pypa-manylinux2014_x86_64:
    image: quay.io/pypa/manylinux2014_x86_64
    volumes:
      - type: bind
        source: ..
        target: /work
    working_dir: /work
    env_file:
      - .env
    command:
      - /bin/bash
      - tests/test.sh
